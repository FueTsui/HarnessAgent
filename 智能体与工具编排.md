# 智能体与工具编排


## 1. 智能体与工具编排

原稿第 1 页

Marcus Stallworth · Builder AI 部署经理
2026 年 8 月


## 2. 课程路线

原稿第 2 页

| 主题 | 要解决的问题 |

| --- | --- |

| 智能体、工作流与聊天机器人 | 何时使用每种模式？智能体有哪些不同？ |

| 智能体循环与平台 | 模型如何决策、使用工具并接入平台？ |

| 选择编排方式 | 如何选择直接调用、PTC、多智能体、SDK 或托管运行时？ |

| 可靠性与实践 | 用架构、评估、回退和追踪验证系统；最后反思并查阅资源。 |


## 3. 从理解智能体到选择编排方式

原稿第 3 页

**把概念变成可实施的设计**：选择能够完成任务、复杂度最低的编排方式。

**区分任务与职责**：判断何时使用聊天机器人、工作流或智能体，理解模型、托管代码、应用工具、状态与追踪的关系。

**验证可靠性**：用评估、结果验证、回退和人工监督检验设计；理解原稿所述模型能力。


## 4. 按工作负载选择模型

原稿第 4 页

GPT-5.6 模型家族 · 以下为原稿时点的定位。

**Sol**：旗舰能力：复杂、模糊或后果重大的任务。

**Terra**：均衡能力：以较低价格提供较强表现。

**Luna**：高吞吐效率：原稿定位为最具成本效率的模型。


## 5. 知识测验 1 / 7

原稿第 5 页

什么是智能体？

| 选项 | 内容 |

| --- | --- |

| A | 只根据提示词生成文本的模型。 |

| B | 不包含模型自主选择的固定 API 调用序列。 |

| C | 能够遵循指令、使用工具、遵守护栏并代表用户采取行动的 AI 系统。 |

| D | 存储对话历史的数据库。 |


## 6. 智能体的四个组成部分

原稿第 6 页

智能体能够遵循指令、使用工具，在边界内代表用户行动。

**模型**：理解目标，决定下一步。

**指令**：定义角色、政策、边界与成功标准。

**工具**：获取上下文，或在系统中执行动作。

**护栏**：约束不安全、无效或超出范围的行为。


## 7. 聊天机器人、工作流还是智能体？

原稿第 7 页

| 模式 | 谁决定路径 | 适合的结果 |

| --- | --- | --- |

| 聊天机器人 | 模型主要回答问题、解释内容或起草文字。 | 需要一个答案。 |

| 工作流 | 代码掌握路由、工具、状态和停止条件。 | 执行路径已知。 |

| 智能体 | 选择步骤、调用工具、观察结果，再决定继续、停止或升级处理。 | 决策需要随情况调整。 |


## 8. 智能体质量取决于整个工作结果

原稿第 8 页

聊天质量主要看回答；智能体质量要看变化中的工作流是否完成目标。

**自主行动与控制**：系统能选择动作，因此政策和权限会影响结果。

**安全与后果**：工具调用会产生真实影响，不只是措辞不当。

**可观测性**：检查运行、模型调用、工具输入输出、交接、护栏、审批和重试。


## 9. 跨步骤工作需要状态与可靠工具

原稿第 8 页

**状态管理**：工作上下文需要跨步骤、跨时间延续。

**工具可靠性**：外部 API 可能失败、返回异常数据，结果需要验证。

**面向目标**：智能体需要规划、自我纠正、停止，或请求帮助。


## 10. 智能体循环：决策、行动、观察

原稿第 9 页

图中节点：任务；调用模型；最终答案？；输出；使用工具；结果加入上下文；指令 + 初始上下文

补充说明：正常路径上，runner 持续循环，直到最终输出或因审批暂停；运行限制、护栏或错误也可能终止运行。


## 11. 运行何时结束或暂停？

原稿第 9 页

**正常路径**：没有后续工具工作时返回最终输出；需要审批时暂停。

**其他终止情况**：运行限制、护栏或错误也可能让本次运行停止。


## 12. 知识测验 2 / 7

原稿第 10 页

正常路径上，Agents SDK 的 runner 何时停止循环？

| 选项 | 内容 |

| --- | --- |

| A | 每次模型响应后。 |

| B | 第一次工具调用后。 |

| C | 模型返回最终输出且没有后续工具工作，或运行因审批暂停。 |

| D | 保存了一轮对话后。 |


## 13. 平台把构建、集成与迭代连在一起

原稿第 11 页

OpenAI Agent Platform · 原稿中的端到端平台定位。

**简化**：简化核心逻辑与编排，使用内置工具构建智能体。

**集成**：连接兼容的模型、工具、编排和追踪；外部系统仍需自己的控制。

**迭代**：结合可观测性、评估与优化，持续改进表现。


## 14. 平台的五个核心组成部分

原稿第 12 页

| 组成 | 职责 |

| --- | --- |

| 用户界面 | 可嵌入、可定制的 UI，加速常见交互模式的构建。 |

| API | Responses API 面向智能体应用，包含内置工具。 |

| 编排 | Agents SDK 提供循环、交接、护栏、会话、审批与追踪；Sandbox Agents（beta）补充工作区执行。 |

| 可观测性 | 正常服务端 SDK 路径默认开启内置追踪。 |

| 优化 | 在追踪、评估和优化之间持续迭代。 |


## 15. 客户案例：从请求到业务查询

原稿第 13 页

| 步骤 | 原稿对应组件 |

| --- | --- |

| 客户请求进入 | 聊天入口 |

| 检查政策 | GPT-5.6 Luna |

| 制定计划 | GPT-5.6 Sol |

| 调用业务工具 | CRM、ERP |

补充说明：这是原稿案例中的职责分配，不将示例理解为普遍适用的审批规则。


## 16. 客户案例：从决策到退款确认

原稿第 13 页

| 步骤 | 原稿对应组件 |

| --- | --- |

| 作出决策 | GPT-5.6 Terra |

| 执行工具 | 发起退款 |

| 确认退款 | GPT-5.6 Luna |

补充说明：这是原稿案例中的职责分配，不将示例理解为普遍适用的审批规则。


## 17. Agents SDK 的基本构件

原稿第 14 页

Runner 执行模型调用 → 工具调用 → 观察结果 → 最终输出的循环。

| 构件 | 作用 |

| --- | --- |

| Agent | 模型、指令、工具，以及可选交接。 |

| Handoff | 把控制权交给另一位专家智能体。 |

| Guardrail | 验证输入、输出或工具行为。 |

| Session | 跨运行保存工作上下文。 |

| Tracing | 呈现并调试每次运行。 |


## 18. 从产品界面接入智能体运行框架

原稿第 15 页

图中节点：产品界面；应用 API；Agents SDK 运行框架；Responses API；工具 / MCP；Sessions / RunState；追踪；Sandbox Agents （beta）


## 19. 应用与 SDK 分别负责什么？

原稿第 16 页

**应用掌握业务边界**：接收请求、选择持久化方式、提供工具并作出审批决定，最后接收 RunResult。

**SDK 执行智能体循环**：当前智能体及输入 → 调用模型 → 检查输出；默认通过 Responses API 调用模型。

补充说明：原稿将该 SDK 路径标为正常服务端执行，默认追踪包含模型调用、工具调用、交接与护栏。


## 20. 检查模型输出后的四条路径

原稿第 16 页

| 输出类型 | 下一步 |

| --- | --- |

| 最终输出 | 返回 RunResult 给应用。 |

| 工具调用 | 执行工具 → 将结果加入上下文 → 再调用模型。 |

| 交接 | 切换当前智能体 → 再调用模型。 |

| 智能体作为工具 | 运行专家 → 将结果返回管理者 → 再调用模型。 |


## 21. 审批暂停后恢复同一次运行

原稿第 16 页

工具可能返回中断信息与运行状态；应用保留审批决定权。

图中节点：SDK 工具调用；暂停 / 中断信息 + 运行状态；应用审批 批准或拒绝；恢复同一次运行


## 22. 能力、控制与可选工作区

原稿第 16 页

| 层次 | 组成 |

| --- | --- |

| 托管工具 | 网页搜索、文件搜索、代码解释器。 |

| 函数工具与 MCP | 函数在应用运行时执行；MCP 可为托管远程或 SDK 管理的本地 / 私有连接。 |

| 编排与安全 | 交接、智能体作为工具、护栏、人工审批。 |

| SandboxAgent（beta） | 清单、能力与沙箱客户端；隔离的文件、命令、软件包与快照。 |

补充说明：可选的 Sessions 和 RunState 可配合 result.history、SDK session、conversationId、previousResponseId。它们处理对话历史，不会自动形成长期记忆。


## 23. 知识测验 3 / 7

原稿第 17 页

交接与“将智能体作为工具”的关键区别是什么？

| 选项 | 内容 |

| --- | --- |

| A | 交接把控制权转移给专家；智能体作为工具把结果返回管理者。 |

| B | 交接可以使用工具，智能体作为工具不能。 |

| C | 交接总需要审批，智能体作为工具从不需要。 |

| D | 它们只是同一种模式的两个名称。 |


## 24. 不用 SDK：应用自行维护执行循环

原稿第 18 页

应用负责循环、客户端工具分派、状态策略与自身可观测性。

**接收并路由**：接收请求，将任务发给模型。

**调用与更新**：调用工具，把结果更新到上下文。

**生成响应**：返回结果，应用维护循环和停止条件。


## 25. 不用 SDK：集成与组件也由应用维护

原稿第 18 页

**配置托管工具，构建客户端工具**：原稿示例包括网页搜索、文件搜索、MCP、代码解释器、Skills 和远程 MCP。每个客户端工具都带来权限与维护责任。

**自己构建基础组件**：消息处理、模型接口、工具管理器与上下文管理。


## 26. 使用 SDK：应用仍负责运行与控制

原稿第 19 页

**SDK 统一循环与生命周期**：接收请求 → 路由模型 → 调用工具 → 更新上下文 → 生成响应。

**标准化基础组件**：预置消息处理、模型接口、工具适配器与上下文衔接。

**应用仍运行 SDK 宿主**：保留应用侧控制；SDK 提供交接、护栏、会话、审批和追踪。

补充说明：原稿列出的接入能力包括网页搜索、文件搜索、远程 MCP、代码解释器、Skills、Shell、计算机使用、子智能体和沙箱。


## 27. SDK 接入的工具与执行能力

原稿第 19 页

| 类别 | 原稿列举 |

| --- | --- |

| 检索与连接 | 网页搜索、文件搜索、远程 MCP |

| 代码与环境 | 代码解释器、Shell 工具、沙箱 |

| 工作流与交互 | Skills、计算机使用、子智能体 |


## 28. 选择 Responses API 或 Agents SDK

原稿第 20 页

**Responses API**：以响应为中心：应用自管循环、托管 PTC 或托管多智能体协调；客户端函数仍由应用运行。

**Agents SDK**：应用运行 SDK，并使用其循环、交接、护栏、会话、审批和追踪。适合应用需要掌握编排或已经使用 SDK 的情况。

补充说明：Responses API 路径可以使用 Direct、PTC、多智能体（beta）或自定义循环。


## 29. 长任务中的上下文与成本能力

原稿第 21 页

GPT-5.6 + Responses API · 按原稿时点转述。

| 能力 | 如何帮助工作流 |

| --- | --- |

| 提示词缓存 | 复用匹配的提示词前缀，减少重复输入成本与延迟。 |

| 推理上下文 | 通过受支持的响应状态携带可用推理项，改善跨轮连续性。 |

| 上下文压缩 | 缩减历史上下文，同时保留继续长对话和工具密集运行所需的状态。 |


## 30. 按任务选择编排能力

原稿第 21 页

**程序化工具调用（PTC）**：托管 JavaScript 协调符合条件的工具，减少有界、工具密集工作流中的中间结果。

**多智能体编排（beta）**：任务可以拆为独立工作流时，并行协调子智能体并汇总结果。

补充说明：应根据工作负载选择能力，再用代表性评估检验质量、延迟与成本。


## 31. 直接调用、PTC，还是混合方式？

原稿第 22 页

| 路径 | 适合的工作 | 选择依据 |

| --- | --- | --- |

| 直接工具调用 | 单次查询、自适应判断、审批或写入。 | 模型需要逐步决策。 |

| 程序化工具调用 | 可预测的多路读取；用代码过滤、连接、排序、验证、去重或聚合。 | 把大量结果归并为紧凑输出。 |

| 混合方式 | PTC 负责检索与归并，直接调用负责判断、审批或一次安全回退。 | 交接点明确。 |


## 32. PTC 负责编排，工具所属方负责执行

原稿第 23 页

托管 JavaScript 协调符合条件的工具；应用掌握客户端工具的凭据、权限、验证与防护。

图中节点：模型编写 JS；JS 请求工具；客户端调用暂停；应用执行工具；JS 恢复并归并


## 33. 知识测验 4 / 7

原稿第 24 页

哪一种请求最适合程序化工具调用？

| 选项 | 内容 |

| --- | --- |

| A | 查询一条客户记录。 |

| B | 审批并执行退款。 |

| C | 获取 100 条只读记录，过滤并聚合，再返回一个紧凑结果。 |

| D | 每得到一个工具结果，都重新选择下一步动作。 |


## 34. 工具是一份契约

原稿第 25 页

契约还必须说明权限、错误行为，以及哪些调用方可以使用。

| 类别 | 职责 |

| --- | --- |

| 函数工具 | 以严格类型的输入、输出封装代码或服务调用。 |

| 托管工具 | 按需使用搜索、文件、代码、Shell 或计算机使用。 |

| MCP 工具 | 通过统一的工具调用协议暴露外部系统。 |

| 结果验证 | 在每个工具结果驱动下一步之前检查它。 |


## 35. 护栏、外部状态与人工控制

原稿第 26 页

**护栏**：配置的检查触发时，验证并阻断偏题、不安全、无效或违反政策的行为。

**外部状态**：在提示词之外保存持久上下文，每步仅注入所需内容。

**人工控制**：风险高、不可逆或含糊的动作需要审批或升级处理。


## 36. 应用侧会话策略

原稿第 27 页

| 策略 | 优势与代价 | 下一轮传入 |

| --- | --- | --- |

| result.history | 应用控制最大；应用负责保存并回放历史。 | 可重新输入的历史记录 |

| session | 持久记忆与可恢复运行；应用拥有会话存储。 | 同一个 session |

补充说明：原稿使用 TypeScript 命名。对应 Python 历史结果接口为 result.to_input_list()。


## 37. 服务端会话策略

原稿第 27 页

| 策略 | 优势与代价 | 下一轮传入 |

| --- | --- | --- |

| conversationId | 共享、持久的服务端状态；状态存放在 Conversations API。 | 会话 ID + 新一轮内容 |

| previousResponseId | 最轻量的延续方式；跟随上一条响应。 | 响应 ID + 新一轮内容 |

补充说明：Python 对应字段为 previous_response_id。


## 38. 每个对话选择一种状态策略

原稿第 27 页

**避免重复注入上下文**：混合本地历史回放与服务端状态，可能重复上下文。

补充说明：对于持久记忆、可恢复的审批流程，或应用自主管理存储，原稿建议默认使用 session。


## 39. 知识测验 5 / 7

原稿第 28 页

持久记忆、可恢复审批和应用自管存储，默认选哪种策略？

| 选项 | 内容 |

| --- | --- |

| A | result.history（TypeScript）/ result.to_input_list()（Python） |

| B | session |

| C | conversationId |

| D | previousResponseId（TypeScript）/ previous_response_id（Python） |


## 40. 让评估决定架构何时升级

原稿第 29 页

**确定性工作流**：代码掌握路径；模型在已知步骤中提供帮助。

**单个智能体 + 工具**：一个智能体选择工具，管理受约束的任务。

**智能体网络**：分流智能体将请求路由给具有专用工具的专家。

补充说明：只有评估证明现有结构不足时才向右推进。PTC 优化可预测且工具密集的阶段；多智能体适合可分离的专家推理。


## 41. 五种高级编排模式

原稿第 30 页

| 模式 | 适用关系 |

| --- | --- |

| 工作流规划 | 把宽泛任务拆成具有依赖关系的有序阶段。 |

| 分流交接 | 把请求路由给范围明确的专家。 |

| 并行执行 | 同时运行独立子任务以降低延迟。 |

| 重试或回退 | 模型、工具或步骤失败时调整策略。 |

| 评估器—优化器 | 依据质量标准检查输出，再进行有界修改。 |

补充说明：组合模式时，每个角色都应有范围狭窄、明确的契约。


## 42. 工作流规划：跟踪阶段与依赖

原稿第 31 页

拆分复杂任务，跟踪依赖，完成当前阶段后再进入下一阶段。

图中节点：用户请求；规划者；执行者；输出

补充说明：依赖关系需要显式控制时使用规划；若执行图固定，确定性工作流可能更简单。


## 43. 分流交接：由专家接管请求

原稿第 32 页

图中节点：用户请求；分流智能体；专家 1；专家 2；专家 3；输出 1；输出 2；输出 3

补充说明：按职责范围、指令、工具、权限和实际测得的表现选择专家。专家具备专用上下文与工具，接管请求并响应。


## 44. 并行执行：独立工作，统一汇总

原稿第 33 页

图中节点：用户请求；任务分配者；任务 1；任务 2；任务 3；汇总者；输出

补充说明：独立智能体并行处理不同证据或专家分支；一个负责最终结果的主智能体协调冲突并形成回答。顺序依赖或共享可变状态更适合单个智能体。


## 45. 重试或回退：限定失败范围与预算

原稿第 34 页

图中节点：用户请求；主路径；输出；回退路径；回退输出

补充说明：在预算内重试暂时失败。只有评估支持时才切换回退策略。契约、审批或后果重大的失败不可盲目重试，应升级处理或以阻断方式终止。


## 46. 评估器—优化器：按标准检查输出

原稿第 35 页

图中节点：用户请求；生成器；评估器；输出

补充说明：使用明确、针对任务的标准。失败时请求有界修改或升级处理。评估器本身必须独立可靠，换一个模型并不能证明可靠性。


## 47. 通过追踪与评估持续改进

原稿第 36 页

**记录与检查**：追踪执行过程，检查真实运行。

**评估与收紧**：评估表现，收紧指令、契约或控制。

**重新测试**：再次验证；只有可测量的失败才支持增加复杂度。

补充说明：原稿闭环为 Trace → Inspect → Evaluate → Tighten → Retest。起步保持简单，用仪表与评估改进，而非凭感觉。


## 48. 复杂系统要分环节评估

原稿第 37 页

| 对象 | 检查内容 |

| --- | --- |

| 检索 | 生成前评估检索质量；关注 Recall@k、Precision@k、MRR、NDCG@k 等排序指标。 |

| 智能体系统 | 是否调用了正确工具或智能体？是否按预期交接、审批？自主程度下的工作流行为是否正确？ |

| 多模态 | 图像需要视觉依据，如 VLM 检查；语音需要基于音频的评估，转写文本只是信号，不是真值。 |


## 49. 用同一任务比较编排路径

原稿第 38 页

先保证质量与证据，再比较效率。

图中节点：直接调用基线；有界 PTC 阶段；明确的混合路径；检查答案与追踪；测量成本与延迟


## 50. 知识测验 6 / 7

原稿第 39 页

正常服务端 SDK 路径中，默认追踪通常包括什么？

| 选项 | 内容 |

| --- | --- |

| A | 只有最终答案。 |

| B | 只有工具错误与重试。 |

| C | 整体运行、模型调用、工具调用及输出、交接和护栏，以及应用添加的自定义 span。 |

| D | 只有应用手动记录的事件。 |


## 51. 智能体技术栈的新能力

原稿第 40 页

开放运行框架 · Skills · 托管 Shell · 沙箱（beta）


## 52. 开放、可定制的 SDK 运行框架

原稿第 41 页

**定义工作，交给 runner 执行循环**：描述要完成的任务，让智能体决定模型与工具循环中的实现细节，减少团队重复工程工作。

**适配自己的基础设施**：开放的运行框架可按已有基础设施与工作流调整。

**升级仍需要维护**：采用模型或运行框架更新时可以获益；集成、配置和评估仍需持续维护。


## 53. Skills 把可重复工作打包

原稿第 42 页

| 组成 | 用途 |

| --- | --- |

| 版本化文件包 | 封装托管或本地 Shell 中可重复使用的工作流。 |

| 必需的 SKILL.md | 清单前置字段定义 name 与 description，说明何时、如何使用。 |

| 托管方式 | 上传后通过 skill_reference 使用。 |

| 本地方式 | 使用已经审查的路径。 |

补充说明：Skills 是高权限指令与代码，应进行审查；需要可复现时固定版本。


## 54. 复用技能也要固定边界

原稿第 42 页

**需要可复现时固定版本**：版本化文件包明确本次任务使用的工作流。

**按高权限指令与代码审查**：检查技能内容，再通过托管引用或已审查本地路径使用。


## 55. 托管 Shell 在隔离容器中运行命令

原稿第 43 页

**通过 Responses API 执行**：模型可在隔离容器中运行命令行级任务。

**转换与分析**：处理 CSV、JSON、日志，执行构建、测试、lint 或仓库分析。

**确定性工作流**：使用挂载文件和 Skills 完成可重复步骤。


## 56. 自动容器与可复用容器

原稿第 43 页

**自动模式**：每次请求使用新容器。
环境标识：container_auto。

**可复用模式**：仅在容器仍活跃时复用。
环境标识：container_reference。


## 57. 沙箱状态与对话状态需要分开

原稿第 44 页

Agents SDK 沙箱（beta） · 三者是不同的状态层面。

**对话上下文**：当前交互所需的信息。

**RunState**：运行进度与恢复相关状态。

**沙箱工作区**：隔离环境中的文件和工作产物。

补充说明：文件与产物可通过复用沙箱状态、快照或持久存储继续使用，具体取决于配置。


## 58. 明确环境与访问范围

原稿第 44 页

**开发者决定可用资源**：选择文件、API 或只读内部系统等资源。

**保留工作进度**：通过复用状态、快照或持久化存储延续文件与产物。

**降低环境依赖**：明确的沙箱减少对本机配置、或任务结束就消失的临时工作区的依赖。


## 59. 交接改变负责人，沙箱改变工作地点

原稿第 45 页

**需要专家推理：交接**：把任务交给具备合适指令、知识或工具的智能体。交接改变当前智能体与响应负责人。

**需要执行环境：沙箱**：为文件检查、命令执行、中间产物和多步骤工作提供隔离工作区。


## 60. 两个示例：合同问题与财务分析

原稿第 45 页

**合同问题交给专家**：分流智能体把合同问题交给法律审查智能体。

**财务分析使用工作区**：打开数据室，计算上传文件并输出报告；经配置后，可从复用会话状态或快照恢复。


## 61. 知识测验 7 / 7

原稿第 46 页

何时应使用 SandboxAgent？

| 选项 | 内容 |

| --- | --- |

| A | 每次生成简短文本响应时。 |

| B | 只要需要记住对话历史时。 |

| C | 需要隔离工作区处理文件、命令、软件包或可恢复产物时。 |

| D | 只有没有工具可用时。 |


## 62. 反思：你会从哪种路径开始？

原稿第 47 页

面对一个范围明确、以读取为主的任务，你会先选直接调用、PTC，还是多智能体？

什么证据足以支持增加复杂度？


## 63. 参考资源：构建与编排

原稿第 48 页

| 方向 | 原稿资源 |

| --- | --- |

| 入门 | Building agents track；构建智能体实用指南 |

| SDK | Agents SDK 指南；Python SDK；TypeScript SDK |

| 编排 | 程序化工具调用指南；Multi-agent Responses（beta） |


## 64. 参考资源：环境与评估

原稿第 48 页

| 方向 | 原稿资源 |

| --- | --- |

| 工作环境 | Sandbox Agents（beta） |

| 可复用工作流 | Skills；Shell |

| 可靠性 | Agent evals 与 trace grading |

补充说明：原稿内嵌链接完整保存在来源索引，未替换为其他资源。


## 65. 谢谢

原稿第 49 页

智能体与工具编排 · 2026 年 8 月